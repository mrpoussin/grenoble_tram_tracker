# DEPRECATED

This folder has been deprecated. The Next.js application has been moved to the project root.

Please use the root folder instead.

## Migration Notes

The following files/folders have been deprecated:
- `next-app/` → moved to root as `src/` and config files
- `public/` → renamed to `public_deprecated/`
- `server.js` → renamed to `server.deprecated.js`

## Running the App

From the project root:
```bash
npm install
npm run dev    # Development
npm run build  # Production build
npm start      # Production server
```
